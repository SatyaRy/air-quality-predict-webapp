# Python-specific
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
*.egg
pip-wheel-metadata/
*.whl

# Virtual environments
venv/
.env/
env/
.venv/
virtualenv/

# Environment variables and secrets
.env
*.env
*.env.*
!.env.example  # Keep example env files, if any

# FastAPI-specific
# Cache and temporary files
*.cache
*.log
*.log.*
logs/
*.pid

# Dependency lock files (optional, include if you don't want to commit them)
# poetry.lock
# requirements.txt (if generated dynamically)

# Testing and coverage
.coverage
.coverage.*
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
tox/
nox/

# IDE and editor files
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
*.swp
*~

# OS-generated files
.DS_Store
Thumbs.db
*.bak
*.tmp

# MongoDB-specific
# Local MongoDB data (if running MongoDB locally)
data/
mongod.lock
*.tgz
*.tar.gz
diagnostic.data/
journal/
mongod/

# Docker (if using Docker for MongoDB or FastAPI)
docker-compose.yml.local
*.dockerignore
Dockerfile.local
*.container
*.volume

# Miscellaneous
*.bak
*.backup
*.temp
*.tmp
temp/
tmp/

# Project-specific exclusions (add custom patterns here)
# Example: exclude local database dumps or backups
*.dump
*.bson
*.backup
.vercel